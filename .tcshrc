
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
if ( -f "/root/miniconda3/etc/profile.d/conda.csh" ) then
    source "/root/miniconda3/etc/profile.d/conda.csh"
else
    setenv PATH "/root/miniconda3/bin:$PATH"
endif
# <<< conda initialize <<<

